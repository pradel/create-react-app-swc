{
  "private": true,
  "scripts": {
    "prettier": "prettier --write \"**/*.{js,ts,tsx,css,scss,json,md,mdx,yml}\"",
    "release": "standard-version -a && yarn workspace craco-swc npm publish"
  },
  "prettier": {
    "singleQuote": true
  },
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "devDependencies": {
    "prettier": "2.5.0",
    "standard-version": "9.3.2"
  }
}
